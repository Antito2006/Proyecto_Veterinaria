class Usuario {

    constructor(
        idUsuario,
        usuario,
        contrasena
    ) {

        this.idUsuario = idUsuario;
        this.usuario = usuario;
        this.contrasena = contrasena;
    }
}

module.exports = Usuario;